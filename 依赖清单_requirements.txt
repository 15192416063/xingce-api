# 行测相似题检索系统 — Python 依赖
# 安装：pip install -r requirements.txt
# 建议 Python 3.10+

# ---- 核心：PDF 处理 ----
pdfplumber>=0.11.0        # 文字版 PDF 提取
PyMuPDF>=1.24.0           # 图推题整页渲染（二期，多模态用）

# ---- 核心：RAG / LLM / 向量 ----
langchain>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0   # 用 OpenAI 时需要；换国产模型见 国产模型接入示例.py
chromadb>=0.5.0           # MVP 本地向量库；产品化换 Qdrant/Milvus

# ---- 界面 ----
streamlit>=1.38.0         # MVP 快速原型界面

# ---- 配置 ----
python-dotenv>=1.0.0      # 读取 .env 里的 API key

# ===== 说明 =====
# 1. 换国产模型（通义/智谱/DeepSeek）时，多数兼容 OpenAI 接口，
#    仍用 langchain-openai，只改 base_url 和 model，见「国产模型接入示例.py」。
# 2. 若用本地 embedding（如 bge 中文模型），需额外安装：
#    pip install sentence-transformers
# 3. 产品化阶段按需追加：qdrant-client / pymilvus 等。
